{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/RiskControl.Identity/RiskControl.Identity.Infrastructure/RiskControl.Identity.Infrastructure.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}